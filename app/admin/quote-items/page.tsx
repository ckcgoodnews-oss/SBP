'use client';
export default function Page() {
  return <main style={{ padding: 24 }}><h1>Quote Items</h1><p>API is wired at <code>/api/admin/quote-items</code>. Item grid/detail editor comes in the next build.</p></main>;
}
